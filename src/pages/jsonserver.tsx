import React from "react";
import Button from "@material-ui/core/Button";

export default function jsonserver(){
  return(
    <div>
      <Button variant="contained" color="primary" size="small">getdata</Button>
    </div>
  );
}